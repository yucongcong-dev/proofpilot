# ProofPilot demo script

1. Enter: `Launch a small online workshop next month`.
2. Show the five-step plan: finish line, inputs, execution, verification, and follow-up.
3. Point out that every step contains evidence and a risk, not just an action.
4. Explain that the deterministic fallback keeps the demo reproducible; `GEMINI_API_KEY` enables Gemini 2.5 Flash for richer plans.
5. Open `/health` and show `{"ok":true}`.
